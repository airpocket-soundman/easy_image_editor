{
  "name": "photobench",
  "private": true,
  "version": "1.0.0",
  "description": "フォトベンチ — 画像編集アプリ(Web/PWA + Capacitorネイティブ)",
  "scripts": {
    "sync-www": "node scripts/copy-www.mjs",
    "android": "npm run sync-www && npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "media-lib": "file:./media-lib"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
